# Codex Chain Step 2

{{include:prompts/_shared/runtime_context.md}}

## Required Edit

Open the workspace file named `{{input.chain_file_name}}`.

Keep the existing first marker line:

```text
{{input.codex1_marker}}
```

Append this exact second marker line if it is not already present:

```text
{{input.codex2_marker}}
```

Do not remove existing content. Do not edit unrelated files.

{{include:prompts/_shared/status_contract.md}}
